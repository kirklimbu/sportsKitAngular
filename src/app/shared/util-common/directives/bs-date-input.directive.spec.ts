import { BsDateInputDirective } from './bs-date-input.directive';

describe('BsDateInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BsDateInputDirective();
    expect(directive).toBeTruthy();
  });
});
