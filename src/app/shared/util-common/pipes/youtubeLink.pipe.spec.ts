import { YoutubeLinkPipe } from './youtubeLink.pipe';

describe('YoutubeLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
