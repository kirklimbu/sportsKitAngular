import { NepaliDateFormatterPipe } from './nepali-date-formatter.pipe';

describe('DateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliDateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
